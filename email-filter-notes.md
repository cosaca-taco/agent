# メール重要度フィルタ 学習メモ

このファイルは、毎日のメールチェック用エージェント（Routine）が読み書きする
「学習済みルール」の置き場所です。ユーザーが実際にフラグ（スター/ラベル）を付けた
メールから傾向を学習し、ここに反映していきます。

## フラグ用ラベル

ユーザーがメールに判断結果を教える手段として、以下の2つのGmailラベルを用意した。

- `エージェント/重要`（Label_3）: ユーザーが「これは仕事上重要」と判断したメールに付ける
- `エージェント/重要じゃない`（Label_4）: ユーザーが「これは重要ではない」と判断したメールに付ける

毎回のチェック時に、前回チェック以降にこの2つのラベルが新しく付けられたメールを検索し、
送信者・件名・本文の傾向を学習する（正例・負例の両方が得られる）。
また、ユーザーがこの会話で直接「このメールは重要/重要じゃない」と教えてくれた場合も、
同様にここへ反映する。

## 現在の判定ルール

（まだ学習データがありません。上記ラベルが付いたメール、またはユーザーからの
直接のフィードバックをもとに、送信者・件名・本文の傾向を分析し、下記に追記してください。）

## 学習履歴

- 2026-09-04: 初期セットアップ。まだ学習データなし。
- 2026-09-04: フラグ付け用ラベル「エージェント/重要」「エージェント/重要じゃない」をGmailに作成。
